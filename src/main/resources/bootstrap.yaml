spring:
  cloud:
    vault:
      token:
      uri:
      kv:
        enabled: true
        application-name:
        default-context:
      fail-fast: true
      generic:
        enabled: false
      config:
        lifecycle:
          enabled: false
      session:
        lifecycle:
          enabled: false
      enabled: false
